<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.tetris20">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Tetris20">
        <activity android:name=".GameOverActivity"></activity>
        <activity android:name=".WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SelectActivity" android:parentActivityName=".WelcomeActivity"></activity>
        <activity android:name=".MainActivity" android:parentActivityName=".SelectActivity"></activity>
        <activity android:name=".ScoreBoardActivity" android:parentActivityName=".WelcomeActivity"></activity>
    </application>

</manifest>